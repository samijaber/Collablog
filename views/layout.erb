<!doctype html>
<html>

<head>
	<link rel="stylesheet" href="/foundation.css" type="text/css">
	<link rel="stylesheet" href="/layout.css" type="text/css">
	<link href='http://fonts.googleapis.com/css?family=Raleway' rel='stylesheet' type='text/css'>
</head>

<title>
	DogeBarks
</title>

<body>

<main>
<h1>
<img class="doge" src="/images/doge.jpeg">
DogeBarks
<img class="doge" src="/images/doge.jpeg">
</h1><!-- </a> -->
	<%unless session[:info].nil? or session[:info][:image].nil? or session[:info][:image].empty? %><img id="avatar" src="<%= session[:info][:image] %>"><% end %>
	<% unless flash[:message].nil? %>
        <div><%= flash[:message] %></div>
        <% end %>
<%= yield %>
</main>
</body>
<script>
function imgify(text) {
    var urlRegex = /(https?:\/\/[^\s]+)/g;
    return text.replace(urlRegex, function(url) {
        return '<a href="' + url + '"><img onerror="this.style.display=\'none\'" src="' + url + '">' + url + '</a>';
    })
}
var tweets = document.querySelectorAll("div.tweet");
for(var i = 0; i < tweets.length; i++)
{
  tweets[i].innerHTML = imgify(tweets[i].innerText);
}
</script>
<footer>
made with love by <a href="http://github.com/samijaber">@samjabz</a> and <a href="http://github.com/tlwr">@tlwr</a>
</footer>
</html>
